# Copy this file to /opt/gridwords-bot/deployment.env. It intentionally contains no secrets.
# Only explicit immutable GHCR tags are allowed; never use latest for a deployment.
BOT_IMAGE=ghcr.io/venomenon328/gridwords-bot:sha-replace-with-40-lowercase-hex-characters
