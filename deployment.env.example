# Format reference only. deploy.sh creates deployment.env atomically after the first
# successfully verified deployment. Do not pre-create it for an empty server.
# Only explicit immutable GHCR tags are allowed; never use latest.
BOT_IMAGE=ghcr.io/venomenon328/gridwords-bot:sha-replace-with-40-lowercase-hex-characters
