package de.venomenon.gridwordsbot.domain.reporting;

/** The only planning outcomes for a due periodic report period. */
public enum PeriodicReportReconciliationAction {
    DELIVER_OR_RECONCILE,
    EXPIRE
}
